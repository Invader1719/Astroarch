\documentclass[11pt]{article}
\usepackage{amsmath,amssymb,mathtools}

% Шрифт — DejaVu Serif: штатный в образе, доп. пакеты не нужны
\usepackage{fontspec}
\setmainfont{DejaVu Serif}

\usepackage[russian]{babel}

% Минимальные поля
\usepackage[a4paper,top=2.5cm,bottom=1.5cm,left=1.5cm,right=1.5cm]{geometry}

\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{graphicx,caption}
\usepackage{needspace}
\usepackage{titlesec}
\usepackage{wasysym}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

% Малиновый — цвет ссылки на сайт в колонтитуле
\definecolor{astroarchpink}{HTML}{E3116C}

% Заголовки — компактные
\titleformat{\section}{\normalfont\large\bfseries}{\thesection:}{0.5em}{}
\titlespacing*{\section}{0pt}{0.8em}{0.3em}

\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection.}{0.5em}{}
\titlespacing*{\subsection}{0pt}{0.5em}{0.2em}

% Списки — компактные
\setlist[itemize]{noitemsep, topsep=0pt, left=1.2em}
\setlist[enumerate]{noitemsep, topsep=0pt, left=1.2em, label=\arabic*.}

% компактные отступы вокруг обтекаемых рисунков и расстояние между колонками
\setlength{\intextsep}{0.5em}
\setlength{\columnsep}{1em}

% аккуратные подписи (не растягивать по ширине)
\captionsetup{font=small,labelfont=bf,justification=raggedright,singlelinecheck=false}

% Колонтитулы
\pagestyle{fancy}
\fancyhf{}
\lhead{Per Aspera Ad Astra}
\rhead{Страница \thepage}
\renewcommand{\headrulewidth}{0.4pt}
\lfoot{Тема: {{ topic_label }}}
\cfoot{\href{https://astroarch.ru/}{\textcolor{astroarchpink}{Astroarch}}}
\rfoot{Станислав Потапов}
\renewcommand{\footrulewidth}{0.4pt}

\begin{document}

{% for task in tasks %}
\noindent\textbf{Задача {{ loop.index }}.}{% if task.title %} \textbf{{ '{' }}{{ task.title }}{{ '}' }}.{% endif %}{% if include_source and (task.source or task.year or task.grade) %} \textit{({% if task.source %}{{ task.source.name }}{% endif %}{% if task.year %}, {{ task.year }}{% endif %}{% if task.grade %}, {{ task.grade }} класс{% endif %})}{% endif %} {{ task.text | safe }}

{% if include_answer and task.answer %}
\vspace{0.5em}\noindent{\itshape Ответ: {{ task.answer | safe }}\par}
{% endif %}

{% if include_solution and task.solution %}
\vspace{0.5em}\noindent{\itshape Решение: {{ task.solution | safe }}\par}
{% endif %}

\vspace{1.5em}

{% endfor %}

\end{document}
